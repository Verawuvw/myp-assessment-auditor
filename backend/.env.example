# 复制为 .env 并填入真实值；后端启动时会自动加载 backend/.env。
# 已存在的系统环境变量优先级更高（不会被 .env 覆盖）。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_MODEL=deepseek-chat
# MYP_MAX_UPLOAD_BYTES=20971520
# MYP_CORS_ORIGINS=*
